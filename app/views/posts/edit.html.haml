.text-center
  = image_tag @post.image.url(:medium)
.form-wrapper
  = render 'form'
.text-center.edit-links
  = link_to "Delete Post", post_path(@post), method: :delete, data: {confirm: "Are you sure yo want to delete this post?" }
  |
  = link_to "cancel", post_path
